# TODO: Make links open in a new tab.

project:
  type: website
  preview:
    port: 7777
  render:
    - "**/*.qmd"
    - "**/*.ipynb"
    - "!updates/posts_working/**"
    - "!extended_posts/pages_working/**"

metadata-files:
  - _website.yml
  - _format.yml

execute:
  freeze: auto
